# タスクボード（やることは、ここだけ）

> このファイルが、やることの**唯一の一覧**。ログやカルテからは T番号で参照する。第二のリストを作らない。
> 追加・完了は、AIに「タスク追加して」「T003 終わった」と言えばよい。

## 行の形式（崩さない）

```
- [ ] T001 | 持ち場 | 期限 | 内容 | 出典
```

| 位置 | 規則 |
|---|---|
| チェック | `- [ ]` 未完了 / `- [x]` 完了 / `- [?]` AI提案（私の採用待ち） |
| ID | `T`＋3桁の連番。**一度振ったら再利用しない**。AI提案は `P`＋2桁 |
| 持ち場 | `仕事/` のフォルダ名。無ければ `-` |
| 期限 | `YYYY-MM-DD`。無ければ `-` |
| 内容 | 1行。動詞で終える（何をやるかが分かる粒度） |
| 出典 | このフォルダ内の相対パス。無ければ `-` |

---

## 今週

## 今月

## いつか

## 🤖 AI提案（採用待ち）

<!-- AIが点検や会話で見つけたものは、ここに `- [?] P01 | ... ` で置く。私が「採用」と言ったら T番号を振って上へ移す。無断で T にしない -->

## 完了（直近）

<!-- 完了したら `[x]` にしてここへ移す。月1回の棚卸し（organize）で、その月の ログ/YYYY-MM.md 末尾「完了したタスク」へ移す -->
