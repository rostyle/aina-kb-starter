# AGENTS.md（Claude Code 以外のAIエージェント向け）

Codex・その他のコーディングエージェントがこのリポジトリを開いたときに読むファイルです。

## まず最初にやること

**このリポジトリの運用ルールは [`CLAUDE.md`](CLAUDE.md) に書かれています。作業を始める前に、必ず全文を読んでください。**

`CLAUDE.md` が唯一の真実源です。このファイルには最低限の要点しか書きません（同じルールを2か所で管理しないため）。
ルールを変更するときは **`CLAUDE.md` だけを直してください。** このファイルは基本的に書き換えません。

## 最低限、これだけは守る

1. **人の情報は `人物/{名前}.md` にだけ書く。** 他の場所に人のプロフィールを作らない
2. **同じ情報を2か所に書かない。** 既存ファイルに追記できないか先に確認する
3. **元ネタ（`素材/`）と成果物（`資料/`）を混ぜない**
4. **迷ったら `受信箱/` に入れる**
5. **認証情報・機密・他人の個人情報は書かない**
6. 保存は Pull request で上げる。コミットメッセージは日本語

## 手順書（スキル）について

`.claude/skills/` に作業手順が置いてあります。ファイル名がそのまま用途です。

| 手順書 | 用途 |
|---|---|
| `.claude/skills/setup-me/SKILL.md` | 「私について」を質問しながら埋める |
| `.claude/skills/daily-log/SKILL.md` | 今日あったことを記録する |
| `.claude/skills/add-source/SKILL.md` | URL・ファイル・テキストを取り込む |
| `.claude/skills/person-log/SKILL.md` | 会話から人物ファイルを更新する |
| `.claude/skills/organize/SKILL.md` | 受信箱を片付ける・棚卸しする |

これらを自動で読み込む仕組みがない場合は、**該当するファイルを開いて、その手順どおりに作業してください。**
